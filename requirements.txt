# lsfMonitor dependencies.
#
# Install everything (GUI + AI + memPrediction + CLI):
#   pip install -r requirements.txt
#
# Minimal install for the JSON-output CLI (bmonitor_cli) only:
#   pip install pyyaml
# bmonitor_cli needs only the Python standard library plus PyYAML (for parsing
# config/lsf/exit_code.yaml and term_signal.yaml during --diagnose fail). LSF
# and license real-time commands rely on the LSF client (bjobs/bqueues/...) and
# the bundled tools/lmstat binary, not on Python packages.
#
# --- bmonitor_cli (CLI, JSON output) ---
pyyaml==6.0.3

# --- GUI (bmonitor / license_monitor) ---
qdarkstyle==3.2.3
PyQt5==5.15.11
screeninfo==0.8.1
matplotlib==3.10.8
pandas==2.3.2
pexpect==4.9.0

# --- AI helpdesk (RAG + LLM tool-use + cluster analysis) ---
xgboost==3.0.5
tabulate==0.9.0
scikit-learn==1.7.2
imbalanced-learn==0.14.1
gensim==4.3.3
numpy==1.26.4
openai==2.32.0
anthropic==0.96.0
pypdf==6.10.2
faiss-cpu==1.13.2
requests==2.32.5

# --- memPrediction subsystem (sampling/training/prediction web app) ---
flask==3.1.2
flask_restful==0.3.10
elasticsearch==8.19.3
elasticsearch-dsl==8.18.0
es_pandas==0.0.23
tqdm==4.67.1
